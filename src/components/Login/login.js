import React from "react";
import { Link } from "react-router-dom";
import styled from "./login.module.css";

const Login = (props) => {
  return <main className={styled.login}>Login</main>;
};

export default Login;
